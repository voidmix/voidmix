# Ignore rules for Oxfmt, which `vp fmt` reads from .gitignore and .prettierignore.

# Generated route trees. Formatting them would be reverted by the next
# `bun run generate`, producing permanent churn and breaking generated-output
# drift checks.
**/routeTree.gen.ts

# Generated Drizzle migrations and metadata.
packages/db/drizzle/

# Vendored agent skills. These are upstream bytes reproduced verbatim so
# `skills update` can replace them and the lockfile hash stays meaningful.
# Never hand-edit or reformat them; corrections belong in skills/voidmix-infra/.
.agents/
