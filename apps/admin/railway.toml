# Set this service's Railway config path to /apps/admin/railway.toml.
# Keep the Railway Root Directory at the repository root.

[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/admin/Dockerfile"
watchPatterns = ["/apps/admin/**", "/packages/**", "/package.json", "/bun.lock", "/vite.config.ts"]

[deploy]
startCommand = "sh -c 'if [ -f /app/.env ]; then exec node --env-file=/app/.env --enable-source-maps .output/server/index.mjs; else exec node --enable-source-maps .output/server/index.mjs; fi'"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
